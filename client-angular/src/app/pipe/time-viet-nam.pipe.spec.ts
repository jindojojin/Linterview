import { TimeVietNamPipe } from './time-viet-nam.pipe';

describe('TimeVietNamPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeVietNamPipe();
    expect(pipe).toBeTruthy();
  });
});
