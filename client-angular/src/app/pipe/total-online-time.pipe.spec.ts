import { TotalOnlineTimePipe } from './total-online-time.pipe';

describe('TotalOnlineTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalOnlineTimePipe();
    expect(pipe).toBeTruthy();
  });
});
